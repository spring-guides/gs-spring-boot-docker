apiVersion: v1
kind: Service
metadata:
  name: test-service
spec:
  selector:
    app: helloworld-deploy1
  #type: NodePort  
  type: NodePort
  ports:
  - protocol: TCP
    port: 8080
    targetPort: 8080
    nodePort: 30080